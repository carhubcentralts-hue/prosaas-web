/**
 * Asset versioning for cache busting
 * 
 * This constant is used across the application to append to static asset URLs
 * to ensure browsers fetch fresh versions when assets are updated.
 * 
 * Increment this version when updating voice demos or call demos.
 */
export const ASSET_VERSION = 'v5';
